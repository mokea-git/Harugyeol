services:
  hrg-bk:
    build:
      context: .
      dockerfile: Dockerfile
    image: hrg-bk:latest
    container_name: hrg-bk
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch(`http://127.0.0.1:$${process.env.PORT || 3000}/health`).then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
